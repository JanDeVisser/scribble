func f(x: i32) : bool {
  fputs(1, "f(");
  puti(x);
  putln(")");
  return x < 42;
}

func b(x: bool) : bool {
    putln(x ? "true" : "false");
}

func main() : i32 {
    b(f(12) && f(69));
    b(f(69) || f(12));

    b(f(12) || f(69));
    b(f(69) && f(12));
    return 0;
}
